import 'package:flutter/material.dart';

class ViewWebSocketOne extends StatelessWidget {
  const ViewWebSocketOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Center(child: Text("Web Socket")),
        ),
        body: Center(
          child: Container(),
        ));
  }
}
