
import 'package:flutter/material.dart';

class ViewGradient extends StatelessWidget {
  const ViewGradient({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}