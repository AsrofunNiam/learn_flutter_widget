import 'package:flutter/material.dart';

class ViewReorderlabelOne extends StatelessWidget {
  const ViewReorderlabelOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Center(child: Text("Re Order one ")),
        ),
        body: Center(
          child: Container(
            
          ),
        ));
  }
}