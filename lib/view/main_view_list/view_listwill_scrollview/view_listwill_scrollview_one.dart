import 'package:flutter/material.dart';

class ViewListwillOne extends StatelessWidget {
  const ViewListwillOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Center(child: Text("Listwill Scrollview one ")),
        ),
        body: Center(
          child: Container(
            
          ),
        ));
  }
}