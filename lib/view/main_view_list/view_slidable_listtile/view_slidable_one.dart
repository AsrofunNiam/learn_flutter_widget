import 'package:flutter/material.dart';

class ViewSlidableOne extends StatelessWidget {
  const ViewSlidableOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Center(child: Text("Slidable One ")),
        ),
        body: Center(
          child: Container(
            
          ),
        ));
  }
}