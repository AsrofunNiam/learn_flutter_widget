import 'package:flutter/material.dart';

class ViewDataTablesOne extends StatelessWidget {
  const ViewDataTablesOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Center(child: Text("Data Tables one ")),
        ),
        body: Center(
          child: Container(
            
          ),
        ));
  }
}